var config = {
    type: Phaser.AUTO,
    width:800,
    height: 600,
	parent: "container",  			//contenedor del juego(id)
	backgroundColor: "#000",
    scene: []
};

var game = new Phaser.Game(config);
